{
  "name": "@schrodinger/common",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "npm run clean && tsc && tsc-alias",
    "clean": "rimraf ../../build/common"
  }
}
